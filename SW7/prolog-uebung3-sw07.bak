;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-intermediate-reader.ss" "lang")((modname prolog-uebung3-sw07) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor mixed-fraction #f #t none #f () #f)))
(define (redouble a-list)
  (local(
         )
    )
  )

; ------------ uebung 1 ------------


; ------------ uebung 2 ------------
(define (fib n)
(cond
  ((or (= n 0) (= n 1)) n)
  (else (+ (fib (- n 1))
           (fib (- n 2)))
        )
  )
)

; a): Rekursion mit Akkumulator
(define (fibII n)
  (local(
         
         )
   )
  )